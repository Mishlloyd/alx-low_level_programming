#include "main.h"
/**
 * _abs - computes the absolute valuse
 * of an integer.
 * @i: input number as an intrger.
 * Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
	return (i);
	}
	else
	{
	return (i * -1);
	}
}
